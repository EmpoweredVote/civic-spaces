# The geo shards are generated byte-for-byte by scripts/generate-geo-names.mjs and
# compared byte-for-byte by `npm run geo:check`. CRLF conversion on checkout would
# make that check report drift on every clone made with core.autocrlf=true.
public/geo/*.json text eol=lf
scripts/*.mjs      text eol=lf
