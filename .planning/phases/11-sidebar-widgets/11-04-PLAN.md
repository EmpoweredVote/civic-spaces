---
phase: 11-sidebar-widgets
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - src/components/widgets/ToolsWidget.tsx
  - src/components/Sidebar.tsx
  - src/components/SidebarMobile.tsx
autonomous: false

must_haves:
  truths:
    - "Sidebar shows a Tools for This Community widget with icon cards for live EV tools"
    - "Each tool card opens in a new browser tab"
    - "Tools not yet live are hidden entirely — no coming soon cards"
    - "All three widgets render correctly together in desktop sidebar and mobile collapsible"
  artifacts:
    - path: "src/components/widgets/ToolsWidget.tsx"
      provides: "2-column icon grid of live civic tools"
      exports: ["ToolsWidget"]
  key_links:
    - from: "src/components/widgets/ToolsWidget.tsx"
      to: "external tool URLs"
      via: "anchor tags with target=_blank"
      pattern: "target=\"_blank\""
    - from: "src/components/Sidebar.tsx"
      to: "src/components/widgets/ToolsWidget.tsx"
      via: "renders ToolsWidget"
      pattern: "<ToolsWidget"
---

<objective>
Build the Tools for This Community widget — a 2-column icon grid of live EV ecosystem tools — wire it into the sidebar, and visually verify all three widgets together.

Purpose: Completes the sidebar by connecting users to the broader Empowered Vote tool ecosystem. The visual checkpoint ensures all three widgets work together in the final layout.

Output: ToolsWidget component integrated. All three sidebar widgets live. Visual verification checkpoint.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-sidebar-widgets/11-CONTEXT.md
@.planning/phases/11-sidebar-widgets/11-RESEARCH.md
@.planning/phases/11-sidebar-widgets/11-01-SUMMARY.md
@.planning/phases/11-sidebar-widgets/11-02-SUMMARY.md
@.planning/phases/11-sidebar-widgets/11-03-SUMMARY.md
@src/components/Sidebar.tsx
@src/components/SidebarMobile.tsx
@src/components/widgets/WidgetCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolsWidget + wire into Sidebar</name>
  <files>
    src/components/widgets/ToolsWidget.tsx
    src/components/Sidebar.tsx
    src/components/SidebarMobile.tsx
  </files>
  <action>
1. Create `src/components/widgets/ToolsWidget.tsx`:
   - Define a static `LIVE_TOOLS` config array at the top of the file:
     ```typescript
     const LIVE_TOOLS = [
       {
         name: 'Empowered Compass',
         description: 'Discover where you stand on the issues',
         url: 'https://compassv2.empowered.vote/results',
         icon: 'compass',  // mapped to inline SVG below
       },
       // Only add Essentials and Treasury Tracker if their URLs are confirmed live.
       // Research marked these as LOW confidence. Before adding, verify the URLs
       // by checking if they resolve (curl or fetch). If not confirmed, OMIT them.
       // Do NOT add "coming soon" placeholders — hide non-live tools entirely.
     ]
     ```
   - For each tool, render a card in a 2-column grid (`grid grid-cols-2 gap-2`):
     - Card: `flex flex-col items-center gap-1 p-3 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors cursor-pointer`
     - Icon: inline SVG icon, ~24x24, in `text-gray-600 dark:text-gray-400`. Use simple recognizable shapes:
       - Compass: circle with N/S/E/W points (or reuse the compass SVG from CompassWidget calibration prompt)
       - If Essentials added: a person/ID card icon
       - If Treasury Tracker added: a bar chart icon
     - Label: `text-xs font-medium text-gray-700 dark:text-gray-300 text-center`
     - Link: entire card is an `<a>` with `href={tool.url} target="_blank" rel="noopener noreferrer"`
     - Tooltip on hover (desktop): use `title` attribute on the `<a>` tag with `tool.description`. Simple and accessible.
   - Wrap in WidgetCard with title "Tools for This Community"
   - If `LIVE_TOOLS` array is empty (all tools removed), do NOT render the widget at all

2. Wire into `src/components/Sidebar.tsx`:
   - Import `ToolsWidget`
   - Replace the placeholder WidgetCard for "Tools for This Community" with `<ToolsWidget />`
   - ToolsWidget takes no props — it's self-contained with static config

3. Wire into `src/components/SidebarMobile.tsx`:
   - Import `ToolsWidget`
   - Replace the corresponding placeholder with `<ToolsWidget />`

4. Before finalizing, attempt to verify the tool URLs:
   - `https://compassv2.empowered.vote/results` — confirmed live (used in compass calibration)
   - `https://essentials.empowered.vote` — check with a quick fetch; if 404 or unreachable, omit
   - `https://treasury-tracker.empowered.vote` — check with a quick fetch; if 404 or unreachable, omit
   Only include tools with confirmed-live URLs in the LIVE_TOOLS array.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - All tool links use `target="_blank" rel="noopener noreferrer"`
    - No "coming soon" text anywhere in ToolsWidget.tsx
    - Grep confirms no dead/broken URLs in LIVE_TOOLS
  </verify>
  <done>ToolsWidget renders live tools in a 2-column icon grid. Non-live tools excluded. All links open in new tabs. Widget integrated in both desktop sidebar and mobile collapsible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete sidebar with all three widgets: Issue Alignment Compass (radar chart or calibration prompt), Representing This Community (rep cards), and Tools for This Community (icon grid). Both desktop sidebar column and mobile collapsible section.</what-built>
  <how-to-verify>
    1. Open the app at desktop width (1200px+). Verify:
       - Right sidebar column shows three widget cards stacked vertically
       - Compass widget shows either a purple radar chart (if you have compass data) or a "Calibrate Now" prompt card
       - Representatives widget shows your elected officials with photos, names, and titles — NO party badges anywhere
       - Tools widget shows icon cards in a 2-column grid; clicking opens new tab
       - If sidebar content exceeds viewport, sidebar scrolls independently from the feed
    2. Open at mobile width (< 768px). Verify:
       - No sidebar column visible
       - Below the hero banner, a "Community Sidebar" collapsible header appears
       - Tapping it smoothly expands to show the same three widgets
       - Tapping again collapses with smooth animation
    3. Switch between slice tabs. Verify:
       - No duplicate network requests (check Network tab — hooks fire once, not 6x)
       - Representatives widget stays consistent (same reps on all tabs, since it's jurisdiction-based)
    4. Dark mode: toggle dark mode and verify all widget cards, text, and chart render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- All three widgets render in desktop sidebar
- All three widgets render in mobile collapsible
- No party affiliation displayed anywhere
- No "coming soon" or placeholder text for non-live tools
- All external links open in new tabs
- Dark mode renders correctly across all widgets
- Network tab shows single API calls, not 6x duplicates
</verification>

<success_criteria>
- Tools widget shows only confirmed-live tools in a 2-column icon grid
- All three sidebar widgets (Compass, Representatives, Tools) render together correctly
- Desktop sidebar scrolls independently when content exceeds viewport
- Mobile collapsible section works with smooth expand/collapse animation
- Visual quality meets v3.0 design standard in both light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/11-sidebar-widgets/11-04-SUMMARY.md`
</output>
