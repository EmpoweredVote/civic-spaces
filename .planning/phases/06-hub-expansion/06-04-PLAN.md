---
phase: 06-hub-expansion
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/components/NotificationBell.tsx
  - src/components/NotificationList.tsx
  - src/components/AppShell.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "tsc -p tsconfig.app.json --noEmit passes with zero errors"
    - "Mobile reply notification tap routes to the correct slice tab (not always Federal)"
    - "onNavigateToThread is removed or optional in NotificationList (no dead code)"
  artifacts:
    - path: "src/components/NotificationBell.tsx"
      provides: "onNavigateToSliceThread passed to both desktop and mobile NotificationList"
      contains: "onNavigateToSliceThread={onNavigateToSliceThread}"
    - path: "src/components/NotificationList.tsx"
      provides: "No dead onNavigateToThread parameter"
      contains: "onNavigateToSliceThread"
    - path: "src/components/AppShell.tsx"
      provides: "Type-safe MemberDirectory sliceId access"
      contains: "slices[activeTab as SliceType]"
  key_links:
    - from: "src/components/NotificationBell.tsx (mobile Sheet)"
      to: "src/components/NotificationList.tsx"
      via: "onNavigateToSliceThread prop"
      pattern: "onNavigateToSliceThread=\\{onNavigateToSliceThread\\}"
---

<objective>
Close the three TypeScript errors found by phase verification and fix the mobile notification routing gap.

Purpose: Phase 6 verification scored 3/4 — one functional gap (mobile reply notifications bypass slice routing) and two compiler errors block a clean build. All three fixes are surgical one-line or two-line changes.

Output: Zero TypeScript errors from tsc -p tsconfig.app.json --noEmit; mobile and desktop notification routing are both wired correctly.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hub-expansion/06-03-SUMMARY.md
@src/components/NotificationBell.tsx
@src/components/NotificationList.tsx
@src/components/AppShell.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire onNavigateToSliceThread to mobile Sheet NotificationList and remove dead onNavigateToThread from NotificationList</name>
  <files>
    src/components/NotificationBell.tsx
    src/components/NotificationList.tsx
  </files>
  <action>
    Two targeted edits — do not touch any other code in these files.

    **NotificationBell.tsx — lines 115-119 (mobile Sheet):**

    The desktop NotificationList at lines 95-100 passes both onNavigateToThread and onNavigateToSliceThread. The mobile Sheet NotificationList at lines 115-119 currently passes only onNavigateToThread and is missing onNavigateToSliceThread. Add the missing prop to match the desktop version:

    Before (lines 115-119):
    ```tsx
    <NotificationList
      onClose={() => setIsOpen(false)}
      onOpenProfile={onOpenProfile}
      onNavigateToThread={onNavigateToThread}
    />
    ```

    After:
    ```tsx
    <NotificationList
      onClose={() => setIsOpen(false)}
      onOpenProfile={onOpenProfile}
      onNavigateToThread={onNavigateToThread}
      onNavigateToSliceThread={onNavigateToSliceThread}
    />
    ```

    **NotificationList.tsx — interface and destructure (lines 12-13 and 19):**

    onNavigateToThread is declared in the interface at line 12, destructured at line 19, but never called anywhere in the component body — handleTap uses only onNavigateToSliceThread for reply events. This is dead code causing TS6133.

    Make the prop optional so existing callers are not broken (NotificationBell passes it; making it optional is safer than removing it, which would require touching the callers):

    Before (line 12 in interface):
    ```ts
    onNavigateToThread: (postId: string) => void
    ```

    After:
    ```ts
    onNavigateToThread?: (postId: string) => void
    ```

    Also update the destructure at line 19 to reflect the optional signature — no other change needed since the value is never read.
  </action>
  <verify>
    After edits, run: npx tsc -p tsconfig.app.json --noEmit

    Errors TS2741 (NotificationBell.tsx:115 missing prop) and TS6133 (NotificationList.tsx:19 never read) must not appear in output.
  </verify>
  <done>
    Mobile Sheet NotificationList receives onNavigateToSliceThread. The onNavigateToThread prop is optional (no TS6133). tsc reports no errors for these two files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix TS7053 — type-safe TabKey indexing into Partial<Record<SliceType, SliceInfo>> in AppShell</name>
  <files>src/components/AppShell.tsx</files>
  <action>
    Targeted one-line edit at line 261. Do not touch any other code.

    Context: slices is Partial<Record<SliceType, SliceInfo>> where SliceType = 'federal' | 'state' | 'local' | 'neighborhood' | 'unified'. activeTab is TabKey which additionally includes 'volunteer'. TypeScript rejects slices[activeTab] because 'volunteer' is not in SliceType. The runtime is already safe — when activeTab is 'volunteer', slices['volunteer'] returns undefined, which the optional chain and null coalescing handle correctly. The fix is a cast at the access site. Do NOT add 'volunteer' to SliceType in database.ts — that is Phase 7's domain.

    Before (line 261):
    ```tsx
    sliceId={slices[activeTab]?.id ?? null}
    ```

    After:
    ```tsx
    sliceId={slices[activeTab as SliceType]?.id ?? null}
    ```

    SliceType is already imported via database.ts types. Verify the import at the top of the file before making the edit. If SliceType is not yet imported, add it to the existing import from '../types/database'.
  </action>
  <verify>
    After edit, run: npx tsc -p tsconfig.app.json --noEmit

    Error TS7053 at AppShell.tsx line 261 must not appear in output.

    Also run the full check to confirm all three errors are gone:
    npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "error TS"

    Output must be empty.
  </verify>
  <done>
    AppShell.tsx compiles without TS7053. Full tsc run across tsconfig.app.json produces zero errors. MemberDirectory still receives the correct sliceId for geo tabs and null when on the volunteer tab.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Full TypeScript check: npx tsc -p tsconfig.app.json --noEmit
   Expected: exits 0, zero error lines

2. Spot-check mobile path: inspect NotificationBell.tsx lines 115-119 in the editor to confirm onNavigateToSliceThread is present in the mobile Sheet NotificationList props — matching the desktop version at lines 95-100.

3. Spot-check dead code removal: inspect NotificationList.tsx interface (around line 12) to confirm onNavigateToThread has a ? making it optional, and confirm the component body still calls only onNavigateToSliceThread for reply events.

4. Spot-check AppShell cast: inspect AppShell.tsx line 261 to confirm slices[activeTab as SliceType] is present.
</verification>

<success_criteria>
- npx tsc -p tsconfig.app.json --noEmit exits with zero errors
- src/components/NotificationBell.tsx mobile Sheet NotificationList includes onNavigateToSliceThread prop
- src/components/NotificationList.tsx onNavigateToThread is optional (? suffix) — no TS6133
- src/components/AppShell.tsx line 261 uses activeTab as SliceType cast — no TS7053
- No code from plans 06-01 through 06-03 was modified or reverted
</success_criteria>

<output>
After completion, create `.planning/phases/06-hub-expansion/06-04-SUMMARY.md`
</output>
