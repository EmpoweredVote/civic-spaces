---
phase: 10-photos-and-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260405000000_phase10_storage_photo_url.sql
  - supabase/config.toml
  - src/types/database.ts
  - src/hooks/useAllSlices.ts
  - src/lib/sliceCopy.ts
  - src/components/HeroBanner.tsx
  - src/components/AppShell.tsx
autonomous: true

must_haves:
  truths:
    - "A Supabase Storage bucket named slice-photos exists with public read RLS policy"
    - "The slices table has a nullable photo_url text column"
    - "HeroBanner displays DB photo_url when present, falls back to sliceCopy type default"
    - "Every slice type has a curated type-default photo in sliceCopy.ts (no broken images)"
    - "useAllSlices returns photoUrl on SliceInfo for every slice"
  artifacts:
    - path: "supabase/migrations/20260405000000_phase10_storage_photo_url.sql"
      provides: "Storage bucket creation, RLS policy, photo_url column"
      contains: "slice-photos"
    - path: "supabase/config.toml"
      provides: "Local dev bucket registration"
      contains: "storage.buckets.slice-photos"
    - path: "src/types/database.ts"
      provides: "SliceInfo with photoUrl field"
      contains: "photoUrl"
    - path: "src/hooks/useAllSlices.ts"
      provides: "photo_url in SELECT query and SliceInfo mapping"
      contains: "photo_url"
    - path: "src/lib/sliceCopy.ts"
      provides: "Curated type-default photo URLs per slice type"
      contains: "defaultPhoto"
    - path: "src/components/HeroBanner.tsx"
      provides: "photoUrl prop with fallback logic"
      contains: "resolvedPhoto"
    - path: "src/components/AppShell.tsx"
      provides: "photoUrl prop passed to HeroBanner"
      contains: "photoUrl"
  key_links:
    - from: "src/hooks/useAllSlices.ts"
      to: "civic_spaces.slices.photo_url"
      via: "Supabase .select() includes photo_url"
      pattern: "photo_url"
    - from: "src/components/AppShell.tsx"
      to: "src/components/HeroBanner.tsx"
      via: "photoUrl prop from activeSlice.photoUrl"
      pattern: "photoUrl.*activeSlice"
    - from: "src/components/HeroBanner.tsx"
      to: "src/lib/sliceCopy.ts"
      via: "fallback: photoUrl ?? copy?.defaultPhoto"
      pattern: "photoUrl.*defaultPhoto"
---

<objective>
Create the Supabase Storage bucket, add `photo_url` column to slices, update type-default photos, and wire the full data path from DB through useAllSlices to HeroBanner with fallback logic.

Purpose: Establishes all infrastructure and code wiring so that (a) every slice type displays a curated default photo immediately, and (b) jurisdiction-specific DB photos override the default when present. Plan 10-02 then uploads Bloomington photos and seeds the DB rows.

Output: Migration file, updated config.toml, updated types/hooks/components, curated photo URLs in sliceCopy.ts.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-photos-and-storage/10-CONTEXT.md
@.planning/phases/10-photos-and-storage/10-RESEARCH.md
@src/lib/sliceCopy.ts
@src/components/HeroBanner.tsx
@src/hooks/useAllSlices.ts
@src/types/database.ts
@src/components/AppShell.tsx
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration + config.toml for Storage bucket and photo_url column</name>
  <files>
    supabase/migrations/20260405000000_phase10_storage_photo_url.sql
    supabase/config.toml
  </files>
  <action>
Create the migration file `supabase/migrations/20260405000000_phase10_storage_photo_url.sql` with three sections:

**Section 1 — Storage bucket:**
```sql
INSERT INTO storage.buckets (id, name, public)
VALUES ('slice-photos', 'slice-photos', true)
ON CONFLICT (id) DO NOTHING;
```

**Section 2 — RLS policy for public reads:**
```sql
CREATE POLICY "slice_photos_public_read"
  ON storage.objects FOR SELECT
  TO public
  USING (bucket_id = 'slice-photos');
```
IMPORTANT: Both the bucket INSERT and the SELECT policy are required. Without the SELECT policy, public reads return 403 even on a `public = true` bucket.

**Section 3 — photo_url column on slices:**
```sql
ALTER TABLE civic_spaces.slices
  ADD COLUMN photo_url text;

COMMENT ON COLUMN civic_spaces.slices.photo_url
  IS 'CDN URL for jurisdiction-specific hero photo; NULL = use type-default from sliceCopy.ts';
```

Do NOT include any UPDATE statements for Bloomington rows — those come in Plan 10-02 after photos are uploaded to Storage.

**config.toml update:**
Add a `[storage.buckets.slice-photos]` section after the existing `[storage]` block:
```toml
[storage.buckets.slice-photos]
public = true
file_size_limit = "5MiB"
allowed_mime_types = ["image/jpeg", "image/png", "image/webp"]
```
  </action>
  <verify>
1. Run `npx supabase db reset` locally — no migration errors.
2. Confirm bucket exists: check Supabase Studio at localhost:54323 -> Storage -> slice-photos bucket visible.
3. Confirm column exists: run `SELECT column_name FROM information_schema.columns WHERE table_schema='civic_spaces' AND table_name='slices' AND column_name='photo_url'` — returns 1 row.
  </verify>
  <done>
Migration file creates slice-photos bucket with public read RLS, adds photo_url nullable text column to civic_spaces.slices, config.toml registers bucket for local dev. No migration errors on reset.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire photo_url through types, hook, sliceCopy, HeroBanner, and AppShell</name>
  <files>
    src/types/database.ts
    src/hooks/useAllSlices.ts
    src/lib/sliceCopy.ts
    src/components/HeroBanner.tsx
    src/components/AppShell.tsx
  </files>
  <action>
**1. Update SliceInfo type** (`src/types/database.ts`):
Add `photoUrl?: string | null` to the `SliceInfo` interface (after `siblingIndex`).

**2. Update useAllSlices hook** (`src/hooks/useAllSlices.ts`):
- Add `photo_url` to the `.select()` string: `'id, slice_type, geoid, current_member_count, sibling_index, photo_url'`
- In the mapping loop, add `photoUrl: row.photo_url ?? null` to the SliceInfo object.

**3. Rename and update sliceCopy.ts** (`src/lib/sliceCopy.ts`):
- Rename the `placeholderPhoto` field to `defaultPhoto` in the `SliceCopy` interface (and its JSDoc comment: `/** Type-default hero photo URL — overridden by DB photo_url when set */`).
- Update ALL 6 entries to use `defaultPhoto` instead of `placeholderPhoto`.
- Replace the 6 Unsplash URLs with curated, appropriate photos. Use Pexels direct URLs (no ixid requirement) or keep Unsplash URLs but ensure they have proper sizing params. The existing URLs are acceptable placeholders — the key requirement is that every type has a working URL. Keep the existing URLs but fix the field name.

Specific guidance on photo selection: The existing Unsplash URLs are functional and appropriately themed. Keep them as-is for type defaults. The important change is the field rename from `placeholderPhoto` to `defaultPhoto` to reflect they are permanent type defaults, not temporary placeholders.

**4. Update HeroBanner** (`src/components/HeroBanner.tsx`):
- Add `photoUrl?: string | null` to the `HeroBannerProps` interface.
- Change the photo resolution line from:
  `const photoUrl = copy?.placeholderPhoto ?? null`
  to:
  `const resolvedPhoto = photoUrl ?? copy?.defaultPhoto ?? null`
- Update all references from `photoUrl` to `resolvedPhoto` (the `bgStyle` and `fallbackBg` lines).

**5. Update AppShell** (`src/components/AppShell.tsx`):
- In the HeroBanner JSX (around line 232), add the `photoUrl` prop:
  `photoUrl={activeSlice.photoUrl}`

**IMPORTANT:** Do NOT introduce any `VITE_SUPABASE_URL` references into sliceCopy.ts. Type-default photos use external URLs only. DB-sourced `photo_url` values (which contain Supabase CDN URLs) come from the hook, not from sliceCopy.ts.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with zero errors.
2. Run the dev server (`npm run dev`) — all 6 slice tabs display hero banners with photos (type defaults from sliceCopy.ts, since no DB photo_url values exist yet).
3. Confirm no console errors related to missing images or undefined photoUrl.
4. Inspect React DevTools or add a temporary console.log in HeroBanner to confirm `photoUrl` prop is `null` or `undefined` (expected — no DB values seeded yet) and `resolvedPhoto` falls back to the type default.
  </verify>
  <done>
SliceInfo includes photoUrl. useAllSlices selects photo_url from DB. sliceCopy.ts uses defaultPhoto (renamed from placeholderPhoto). HeroBanner accepts photoUrl prop with two-level fallback (DB -> type default). AppShell passes activeSlice.photoUrl to HeroBanner. TypeScript compiles clean. All 6 tabs show hero photos via type-default fallback.
  </done>
</task>

</tasks>

<verification>
1. `npx supabase db reset` completes without migration errors
2. `npx tsc --noEmit` passes with zero errors
3. `npm run dev` — visit each of the 6 slice tabs, all show hero banner with photo
4. No console errors, no broken image states
5. Bloomington slices show type-default photos (expected — DB seeding is Plan 10-02)
</verification>

<success_criteria>
- Storage bucket and RLS policy exist in migration
- config.toml registers slice-photos bucket for local dev
- photo_url column exists on civic_spaces.slices
- SliceInfo type includes photoUrl
- useAllSlices SELECT includes photo_url
- HeroBanner resolves photo via: DB photoUrl -> sliceCopy defaultPhoto -> null
- All 6 slice tabs display hero photos with no broken image states
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-photos-and-storage/10-01-SUMMARY.md`
</output>
