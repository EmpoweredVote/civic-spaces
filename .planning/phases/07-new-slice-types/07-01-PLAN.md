---
phase: 07-new-slice-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260403000000_phase7_unified_volunteer.sql
  - src/types/database.ts
autonomous: true

must_haves:
  truths:
    - "The slices table accepts 'unified' and 'volunteer' as valid slice_type values"
    - "TypeScript SliceType includes 'volunteer' and SliceInfo includes siblingIndex"
  artifacts:
    - path: "supabase/migrations/20260403000000_phase7_unified_volunteer.sql"
      provides: "CHECK constraint extension for unified and volunteer slice types"
      contains: "unified.*volunteer"
    - path: "src/types/database.ts"
      provides: "SliceType with 'volunteer', SliceInfo with siblingIndex"
      contains: "volunteer"
  key_links:
    - from: "src/types/database.ts"
      to: "supabase schema"
      via: "SliceType union mirrors DB CHECK constraint values"
      pattern: "volunteer"
---

<objective>
Add 'unified' and 'volunteer' as valid slice_type values in the database and extend the TypeScript types to match.

Purpose: All downstream plans (assignment service, frontend activation) depend on the schema accepting the new slice types and TypeScript knowing about them.
Output: One SQL migration file and updated TypeScript types.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-new-slice-types/07-RESEARCH.md

@supabase/migrations/20260327000001_schema.sql
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration — extend CHECK constraint and add NOTIFY</name>
  <files>supabase/migrations/20260403000000_phase7_unified_volunteer.sql</files>
  <action>
Create a new migration file that:

1. DROP the existing CHECK constraint `slices_slice_type_check` on `civic_spaces.slices`
2. ADD a new CHECK constraint with the same name allowing all 6 values: `('federal', 'state', 'local', 'neighborhood', 'unified', 'volunteer')`
3. Add `NOTIFY pgrst, 'reload schema';` at the end (required per project convention — PostgREST must reload after structural changes)

Do NOT insert sentinel rows — the assignment service's `findActiveSliceForGeoid` creates slices on demand (confirmed in research). The existing UNIQUE constraint `slices_geoid_type_sibling_key` handles both new types automatically.

Add a comment block at the top:
```sql
-- Phase 7: New Slice Types
-- Extends the slice_type CHECK constraint to allow 'unified' and 'volunteer'
-- No new RLS policies needed — existing membership-based policies are type-agnostic
-- (confirmed: get_boosted_feed_filtered uses SECURITY INVOKER, posts_select_slice_member gates by slice_id membership)
```
  </action>
  <verify>
Run `npx supabase db diff` or manually inspect the migration SQL. The file should contain exactly:
- DROP CONSTRAINT slices_slice_type_check
- ADD CONSTRAINT slices_slice_type_check with all 6 values
- NOTIFY pgrst, 'reload schema'
  </verify>
  <done>Migration file exists and is syntactically valid SQL. The CHECK constraint allows all 6 slice types.</done>
</task>

<task type="auto">
  <name>Task 2: Extend TypeScript types — SliceType and SliceInfo</name>
  <files>src/types/database.ts</files>
  <action>
Make two changes to `src/types/database.ts`:

1. Add `'volunteer'` to the `SliceType` union type:
   ```typescript
   export type SliceType = 'federal' | 'state' | 'local' | 'neighborhood' | 'unified' | 'volunteer'
   ```

2. Add `siblingIndex: number` to the `SliceInfo` interface:
   ```typescript
   export interface SliceInfo {
     id: string
     sliceType: SliceType
     geoid: string
     memberCount: number
     siblingIndex: number
   }
   ```

Do NOT change any other types. `TabKey` already includes 'volunteer' — confirmed in the codebase.

After editing, run `npx tsc --noEmit` to verify the project compiles. If there are downstream type errors from the new `siblingIndex` required field (e.g., places that construct SliceInfo without it), note them but do NOT fix them — Plan 07-03 handles the frontend wiring.
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | head -30`. Expect either clean build or errors ONLY in files that construct SliceInfo objects (useAllSlices.ts) — those are expected and fixed in Plan 07-03. No errors in database.ts itself.
  </verify>
  <done>SliceType includes 'volunteer'. SliceInfo includes siblingIndex. No regressions in database.ts.</done>
</task>

</tasks>

<verification>
1. Migration file `supabase/migrations/20260403000000_phase7_unified_volunteer.sql` exists with correct DROP/ADD constraint SQL
2. `SliceType` in `src/types/database.ts` includes all 6 values: federal, state, local, neighborhood, unified, volunteer
3. `SliceInfo` in `src/types/database.ts` has `siblingIndex: number` field
4. `npx tsc --noEmit` shows no errors in database.ts (downstream errors in useAllSlices.ts are expected)
</verification>

<success_criteria>
- The database schema accepts 'unified' and 'volunteer' slice_type values after the migration runs
- TypeScript types are the single source of truth for all 6 slice types and include sibling index data
</success_criteria>

<output>
After completion, create `.planning/phases/07-new-slice-types/07-01-SUMMARY.md`
</output>
