---
phase: 12-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProfileFriends.tsx
  - src/components/ProfilePage.tsx
  - src/components/NotificationList.tsx
  - src/components/NotificationBell.tsx
  - src/components/AppShell.tsx
autonomous: true

must_haves:
  truths:
    - "`MutualFriendsList` no longer accepts a `friendCount` prop"
    - "All call sites of `ProfileFriends` pass no `friendCount` prop"
    - "`NotificationListProps` no longer includes `onNavigateToThread`"
    - "All call sites of `NotificationBell` and `NotificationList` pass no `onNavigateToThread`"
    - "`npx tsc --noEmit` exits with zero errors"
  artifacts:
    - path: "src/components/ProfileFriends.tsx"
      provides: "ProfileFriends + MutualFriendsList with friendCount removed"
      contains: "interface ProfileFriendsProps"
    - path: "src/components/NotificationList.tsx"
      provides: "NotificationList with onNavigateToThread removed"
      contains: "interface NotificationListProps"
    - path: "src/components/NotificationBell.tsx"
      provides: "NotificationBell with onNavigateToThread removed"
      contains: "interface NotificationBellProps"
    - path: "src/components/AppShell.tsx"
      provides: "NotificationBell call site cleaned"
  key_links:
    - from: "src/components/ProfilePage.tsx"
      to: "src/components/ProfileFriends.tsx"
      via: "ProfileFriends JSX call"
      pattern: "<ProfileFriends"
    - from: "src/components/AppShell.tsx"
      to: "src/components/NotificationBell.tsx"
      via: "NotificationBell JSX call"
      pattern: "<NotificationBell"
    - from: "src/components/NotificationBell.tsx"
      to: "src/components/NotificationList.tsx"
      via: "NotificationList JSX call"
      pattern: "<NotificationList"
---

<objective>
Remove two dead props that have been vestigial since v2.0: `friendCount` from `MutualFriendsList`/`ProfileFriends`, and `onNavigateToThread` from `NotificationListProps`/`NotificationBellProps`. Both props are accepted but never used. Result: zero TypeScript errors, zero vestigial interfaces.

Purpose: Ship v3.0 Cleanup with a clean type surface — no props that exist only on paper.
Output: Four modified source files, zero `tsc` errors, one commit.
</objective>

<execution_context>
@C:\Users\Chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cleanup/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLEAN-01 — Remove `friendCount` from ProfileFriends + MutualFriendsList</name>
  <files>src/components/ProfileFriends.tsx, src/components/ProfilePage.tsx</files>
  <action>
    In `src/components/ProfileFriends.tsx`, make these four changes:

    1. `ProfileFriendsProps` interface (line ~10): remove `friendCount: number` from the interface. The interface should contain only `userId: string` and `isSelf: boolean`.

    2. `MutualFriendsList` inline type (line ~59): change `{ userId, friendCount }: { userId: string; friendCount: number }` to `{ userId }: { userId: string }`. The `friendCount` parameter is never read — the component derives mutual count from `mutualFriends.length` via the hook.

    3. `ProfileFriends` default export destructuring (line ~89): remove `friendCount` from the destructured parameters. Change `{ userId, isSelf, friendCount }` to `{ userId, isSelf }`.

    4. `MutualFriendsList` call site inside `ProfileFriends` (line ~98): remove `friendCount={friendCount}` from the JSX. The call should be `<MutualFriendsList userId={userId} />`.

    In `src/components/ProfilePage.tsx`:

    5. `ProfileFriends` call site (line ~93): remove `friendCount={stats?.friendCount ?? 0}` from the JSX. The call should be:
    ```tsx
    <ProfileFriends
      userId={userId}
      isSelf={isSelf}
    />
    ```

    DO NOT touch:
    - `ProfileStatsStrip.tsx` — its `friendCount` prop is actively rendered in the stats strip and stays
    - `ProfilePage.tsx` line ~76 — `<ProfileStatsStrip ... friendCount={stats?.friendCount ?? 0} ...>` stays
    - `useProfileStats.ts` — still needed by ProfileStatsStrip
  </action>
  <verify>Run `npx tsc --noEmit` from the project root. Confirm no errors reference ProfileFriends, MutualFriendsList, or friendCount.</verify>
  <done>`ProfileFriendsProps` has no `friendCount` field; `MutualFriendsList` accepts only `{ userId: string }`; `ProfilePage` passes no `friendCount` to `ProfileFriends`; `tsc` reports zero errors from these files.</done>
</task>

<task type="auto">
  <name>Task 2: CLEAN-02 — Remove `onNavigateToThread` from NotificationList, NotificationBell, AppShell</name>
  <files>src/components/NotificationList.tsx, src/components/NotificationBell.tsx, src/components/AppShell.tsx</files>
  <action>
    In `src/components/NotificationList.tsx`:

    1. `NotificationListProps` interface: remove `onNavigateToThread?: (postId: string) => void` from the interface. The interface should contain only `onClose` and `onNavigateToSliceThread`.

    2. Destructuring in the function signature: remove `onNavigateToThread: _onNavigateToThread` from the destructured parameters. The parameter was prefixed with `_` because it was never called — confirm the function body has no reference to `_onNavigateToThread` or `onNavigateToThread` before deleting.

    In `src/components/NotificationBell.tsx`:

    3. `NotificationBellProps` interface: remove `onNavigateToThread: (postId: string) => void` from the interface. The interface should contain only `onNavigateToSliceThread`.

    4. Function destructuring: remove `onNavigateToThread` from the destructured parameters.

    5. Desktop popover `NotificationList` call site (line ~93-98): remove `onNavigateToThread={onNavigateToThread}`. Leave `onClose` and `onNavigateToSliceThread` in place.

    6. Mobile sheet `NotificationList` call site (line ~113-116): remove `onNavigateToThread={onNavigateToThread}`. Leave `onClose` and `onNavigateToSliceThread` in place.

    In `src/components/AppShell.tsx`:

    7. Desktop `NotificationBell` call site (line ~162-168): remove the `onNavigateToThread={...}` prop block entirely. The entire lambda that reads:
    ```tsx
    onNavigateToThread={(postId) => {
      setScrollToLatestMap(prev => ({ ...prev, [activeTab]: true }))
      setActivePostIds(prev => ({ ...prev, [activeTab]: postId }))
    }}
    ```
    should be deleted. Leave `onNavigateToSliceThread={handleNotificationNavigate}` in place.

    DO NOT touch:
    - `SliceFeedPanel.tsx` — its `onNavigateToThread` prop is genuine (used for in-feed thread navigation) and must stay
    - The `onNavigateToThread` props on `SliceFeedPanel` JSX at AppShell lines ~289 and ~314 — those are `SliceFeedPanel` props, not `NotificationBell` props, and must stay
  </action>
  <verify>Run `npx tsc --noEmit` from the project root. Confirm zero errors. Also grep to confirm no remaining `onNavigateToThread` references in NotificationList.tsx, NotificationBell.tsx, or AppShell.tsx (excluding the SliceFeedPanel call sites).</verify>
  <done>`NotificationListProps` has no `onNavigateToThread` field; `NotificationBellProps` has no `onNavigateToThread` field; the `NotificationBell` JSX in AppShell passes no `onNavigateToThread`; `tsc` exits with zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: Full TypeScript verification + commit</name>
  <files></files>
  <action>
    Run `npx tsc --noEmit` from the project root. The expected output is silence (zero errors). If errors appear, fix them before proceeding.

    Once clean, stage and commit:
    ```bash
    git add src/components/ProfileFriends.tsx src/components/ProfilePage.tsx src/components/NotificationList.tsx src/components/NotificationBell.tsx src/components/AppShell.tsx
    git commit -m "refactor(cleanup): remove dead props friendCount and onNavigateToThread (CLEAN-01, CLEAN-02)"
    ```
  </action>
  <verify>`npx tsc --noEmit` exits with no output and exit code 0. `git log --oneline -1` shows the cleanup commit.</verify>
  <done>Zero TypeScript errors. Both dead props are fully removed from all interfaces, destructuring patterns, and JSX call sites. Commit exists on main.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npx tsc --noEmit` — zero errors, zero warnings
2. `grep -n "friendCount" src/components/ProfileFriends.tsx src/components/ProfilePage.tsx` — no matches in the ProfileFriends interface or MutualFriendsList (ProfileStatsStrip/ProfilePage stats strip references are fine)
3. `grep -n "onNavigateToThread" src/components/NotificationList.tsx src/components/NotificationBell.tsx` — zero matches
4. `grep -n "onNavigateToThread" src/components/AppShell.tsx` — matches only from SliceFeedPanel JSX (lines ~289, ~314), none from NotificationBell JSX
</verification>

<success_criteria>
1. `MutualFriendsList` component type is `{ userId: string }` — no `friendCount`
2. `ProfileFriendsProps` interface has no `friendCount` field
3. `ProfilePage` passes no `friendCount` to `ProfileFriends`
4. `NotificationListProps` has no `onNavigateToThread` field
5. `NotificationBellProps` has no `onNavigateToThread` field
6. `NotificationBell` JSX in AppShell passes no `onNavigateToThread`
7. `npx tsc --noEmit` exits with zero errors
8. All changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/12-cleanup/12-01-SUMMARY.md` using the summary template at `@C:\Users\Chris\.claude\get-shit-done\templates\summary.md`.
</output>
